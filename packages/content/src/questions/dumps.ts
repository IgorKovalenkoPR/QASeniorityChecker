import { defineQuestions } from '../define.js';

/**
 * Practice-test style questions, written in the shape used by the widely
 * circulated QA quiz banks (Guru99, ISTQB mock exams, interview question sets):
 * short applied scenarios with one clearly best answer and three plausible
 * distractors.
 *
 * These are ORIGINAL questions written for this bank. Nothing is reproduced
 * from a third-party question set - copying commercial exam items would be both
 * a copyright problem and a quality problem, since public dumps are riddled with
 * outdated and incorrect answers. What is borrowed is the format and the
 * difficulty calibration, not the content.
 */

export const dumpTraineeQuestions = defineQuestions('trainee', 'practice-dump', [
  ['DP-T-001', 'test-design-techniques',
    'A password field accepts 8 to 16 characters. Which set of lengths gives the best boundary coverage?',
    ['7, 8, 16, 17', '8, 12, 16', '1, 8, 16, 100', '0, 8, 16'], 0,
    'Each boundary is paired with its nearest invalid neighbour. 12 adds nothing that 8 and 16 do not already represent.'],
  ['DP-T-002', 'testing-types',
    'A build is delivered at 17:00 and the release is tomorrow. Which testing do you run first?',
    ['Smoke tests on the critical paths', 'The full regression suite', 'Usability testing', 'Performance testing'], 0,
    'The first question is whether the build is viable at all. Everything else is wasted if it is not.'],
  ['DP-T-003', 'severity-priority',
    'The "Buy now" button is invisible on iPhone Safari only. Classification:',
    ['High severity, high priority', 'Low severity, low priority', 'High severity, low priority', 'Low severity, high priority'], 0,
    'Revenue path plus a major browser share: both impact and urgency are high.'],
  ['DP-T-004', 'browser-architecture',
    'A user reports that they are logged out every time they close the browser, although "remember me" was checked. The most likely cause is:',
    ['The session cookie has no expiry, so it is deleted at browser close', 'The password is wrong', 'The server is down', 'JavaScript is disabled'], 0,
    'A cookie without Expires or Max-Age is a session cookie by definition, which is exactly the symptom described.'],
  ['DP-T-005', 'test-artifacts',
    'Which of these is the best expected result for a login test?',
    ['The user is redirected to /dashboard and the header shows their name', 'Login works', 'No errors appear', 'The page loads'], 0,
    'A verifiable, observable outcome. "Works" cannot be failed by anyone but its author.'],
  ['DP-T-006', 'defect-lifecycle',
    'You reported a defect yesterday. Today the build changed and the defect no longer reproduces, but nothing in the changelog relates to it. You should:',
    ['Retest carefully, then note the finding in the ticket rather than closing it silently', 'Close it as not reproducible', 'Delete the ticket', 'Raise its priority'], 0,
    'A defect that vanishes without a known fix usually means the trigger conditions changed, not that the defect is gone.'],
  ['DP-T-007', 'html-css',
    'An element is visible but a click does nothing. Which is the most likely front-end cause to check first?',
    ['Another element overlaps it and receives the click', 'The CSS colour is wrong', 'The font failed to load', 'The page title is missing'], 0,
    'A transparent overlay or a mispositioned modal backdrop is the classic cause, and devtools element inspection confirms it in seconds.'],
  ['DP-T-008', 'testing-types',
    'Which is the best example of negative testing?',
    ['Submitting a form with the email field set to "abc"', 'Submitting a valid form', 'Measuring the submit time', 'Checking the button colour'], 0,
    'Negative testing supplies input the system should reject, and verifies that it rejects it gracefully.'],
  ['DP-T-009', 'test-execution',
    'A test case has 12 steps and fails at step 3. You should:',
    ['Record the failure with evidence at step 3 and report it', 'Continue to step 12 and then report', 'Mark the whole case as blocked', 'Skip to the last step'], 0,
    'The verdict is Failed at step 3. Whether you continue is a judgement call; the report must state where it failed.'],
  ['DP-T-010', 'mobile-platforms',
    'An app works on Android 14 but crashes on Android 9. The report must include:',
    ['The OS versions, devices and the crash log or stack trace', 'Only the crash description', 'Only the Android 9 device model', 'Only a screenshot'], 0,
    'Version-specific crashes are usually API-level defects, and the stack trace is what identifies the call.'],
  ['DP-T-011', 'test-design-techniques',
    'A dropdown has 5 options and a text field accepts 3 valid formats. The minimum number of tests for single-value coverage of both is:',
    ['5', '15', '8', '3'], 0,
    'Covering each value of each parameter at least once needs max(5, 3) = 5 tests, pairing them up. Fifteen would be full combinatorial coverage.'],
  ['DP-T-012', 'defect-reports-en',
    'Which statement belongs under "Steps to reproduce"?',
    ['1. Open /cart with 2 items. 2. Apply code SAVE10. 3. Click Checkout.', 'The total is wrong', 'This is a regression from build 42', 'Severity: Major'], 0,
    'Steps are the numbered actions only. Observations, history and metadata belong in their own fields.'],
  ['DP-T-013', 'browser-architecture',
    'A page shows old content after a deployment. Which check comes first?',
    ['Hard refresh and inspect whether assets are served from cache', 'Reinstall the browser', 'Report a back-end defect', 'Clear the database'], 0,
    'Cache is the overwhelmingly likely cause, and it takes ten seconds to rule out.'],
  ['DP-T-014', 'testing-types',
    'Which check belongs to compatibility testing?',
    ['The same flow on Chrome, Firefox and Safari at three viewport widths', 'Response time under 500 concurrent users', 'Password strength enforcement', 'Database backup and restore'], 0,
    'Compatibility covers browsers, devices, OS versions and co-existing software.'],
  ['DP-T-015', 'test-documentation',
    'Which test case title is best?',
    ['Login fails with a clear message when the password is incorrect', 'Login test 3', 'Check login', 'Password'], 0,
    'A title should state the condition and the expected outcome so a reader can decide whether to run it without opening it.'],
]);

export const dumpJuniorQuestions = defineQuestions('junior', 'practice-dump', [
  ['DP-J-001', 'rest-http',
    'A GET request to /api/users/999 returns 200 with an empty body for a user that does not exist. This is:',
    ['A defect: it should return 404', 'Correct behaviour', 'A performance issue', 'A front-end defect'], 0,
    'Clients cannot distinguish "no such user" from "user with no data". The status code is the contract.'],
  ['DP-J-002', 'databases',
    'Which query counts orders per customer, including customers with none?',
    ['SELECT c.id, COUNT(o.id) FROM customers c LEFT JOIN orders o ON o.customer_id = c.id GROUP BY c.id',
     'SELECT c.id, COUNT(*) FROM customers c JOIN orders o ON o.customer_id = c.id GROUP BY c.id',
     'SELECT c.id, COUNT(o.id) FROM customers c, orders o GROUP BY c.id',
     'SELECT COUNT(*) FROM orders GROUP BY customer_id'], 0,
    'LEFT JOIN keeps the zero-order customers, and COUNT(o.id) counts only non-NULL rows, so they correctly show 0. COUNT(*) would show 1.'],
  ['DP-J-003', 'testing-levels',
    'A defect appears only when module A and module B run together, though each passes its own unit tests. The level that should have caught it is:',
    ['Integration testing', 'Component testing', 'Acceptance testing', 'Unit testing'], 0,
    'Interface and interaction defects are the definition of what integration testing targets.'],
  ['DP-J-004', 'acceptance-criteria',
    'A story says "the user can filter results". Which is the most important question before testing?',
    ['Filter by what, with what combination behaviour and what default?', 'Which colour is the filter button?', 'Who implemented it?', 'When will it be released?'], 0,
    'Without the filter set and its combination semantics, no expected result can be stated.'],
  ['DP-J-005', 'rest-http',
    'A DELETE request is sent twice for the same resource. The second call should return:',
    ['404 or 204 - but never create a side effect', '500', '201', 'A duplicate deletion error that changes state'], 0,
    'DELETE is idempotent: repeating it must leave the same state. Either 204 or 404 is defensible; a side effect is not.'],
  ['DP-J-006', 'ci-systems',
    'The pipeline is green but a defect reached production in a covered area. The most useful investigation is:',
    ['Whether the test asserted the right thing, not just that it ran', 'Whether the pipeline was fast enough', 'Which developer merged it', 'How many tests exist'], 0,
    'A passing test with a weak or wrong assertion is indistinguishable from coverage until something escapes.'],
  ['DP-J-007', 'testing-principles',
    'The team asks you to guarantee that the release has no defects. The correct answer is:',
    ['I can report the risks and coverage; no testing can guarantee the absence of defects', 'Yes, if I test everything', 'Yes, if we automate everything', 'No, testing is pointless'], 0,
    'This is the second testing principle stated as a professional boundary rather than as trivia.'],
  ['DP-J-008', 'web-app-architecture',
    'A user sees a 502 Bad Gateway. This most likely means:',
    ['An upstream server gave an invalid response to the proxy', 'The user entered a wrong URL', 'The browser cache is stale', 'The request body was malformed'], 0,
    '502 is a proxy-level fault: the gateway reached the upstream but got something it could not use.'],
  ['DP-J-009', 'story-verification',
    'You are asked to sign off a story whose acceptance criteria were changed after development finished. You should:',
    ['Verify against the current agreed criteria and flag that they changed mid-story', 'Verify against the original criteria', 'Sign off without verification', 'Refuse to verify'], 0,
    'The current agreement is the oracle, but a silent mid-story change is a process signal worth raising.'],
  ['DP-J-010', 'databases',
    'A UI shows a total of 100.00 but the database stores 99.995. The most likely defect is:',
    ['Rounding or a floating-point type used for money', 'A UI caching problem', 'A network error', 'A permissions issue'], 0,
    'Money in a binary floating-point type is a recurring defect class; decimal or integer minor units is the fix.'],
  ['DP-J-011', 'sdlc-methodologies',
    'In a two-week sprint, when should testing of a story start?',
    ['As soon as the story is testable, in parallel with development', 'In the last two days', 'After the sprint review', 'Only when all stories are done'], 0,
    'Batching testing to the sprint end recreates a waterfall inside the sprint and guarantees carry-over.'],
  ['DP-J-012', 'rest-http',
    'A request without a token returns 200 and the requested data. This is:',
    ['A critical security defect - broken access control', 'Correct if the endpoint is fast', 'A minor documentation issue', 'Expected for GET requests'], 0,
    'Broken access control is consistently the top item in the OWASP Top 10, and it is reachable by ordinary functional testing.'],
  ['DP-J-013', 'testing-levels',
    'Which of these should be caught by unit tests rather than by manual testing?',
    ['An off-by-one error in a date calculation function', 'A confusing button label', 'A slow page under load', 'A broken third-party integration'], 0,
    'Deterministic pure logic is the cheapest thing to cover at unit level and the most expensive to cover manually.'],
  ['DP-J-014', 'oop-principles',
    'In a Page Object framework, putting an assertion inside a page class is usually a mistake because:',
    ['Pages should describe capability; verdicts belong in the tests', 'Assertions are slow', 'It breaks inheritance', 'It prevents parallel execution'], 0,
    'Keeping verdicts in the test keeps the page reusable by tests with different expectations.'],
  ['DP-J-015', 'requirements-testing',
    'A requirement says "the export must support large files". Your first action is:',
    ['Ask for a number: how large, in what format, within what time', 'Test with a 1 GB file', 'Mark the requirement as passed', 'Skip it'], 0,
    'Picking your own threshold quietly invents the requirement and guarantees an argument at acceptance.'],
]);

export const dumpMiddleQuestions = defineQuestions('middle', 'practice-dump', [
  ['DP-M-001', 'auto-api',
    'An API test suite passes locally and fails in CI with intermittent 401s. The most likely cause is:',
    ['A token obtained once and reused past its expiry across parallel workers', 'A slow CI machine', 'A wrong assertion', 'A missing test case'], 0,
    'Shared, cached credentials across parallel workers is the classic source of intermittent auth failures in CI only.'],
  ['DP-M-002', 'auto-performance',
    'Response times are: p50 120 ms, p95 400 ms, p99 6 s. The most important finding to report is:',
    ['1% of requests take 6 seconds - investigate the tail before the mean', 'The average is acceptable', 'p95 is the only relevant number', 'No issue exists'], 0,
    'A 6-second p99 on a busy endpoint is thousands of bad experiences a day, and it usually signals a lock, a cold cache or a slow query path.'],
  ['DP-M-003', 'json',
    'An API returns "created_at": "2026-03-15T02:30:00" with no timezone. Why is this a defect?',
    ['The client cannot know the offset, so the same value renders differently per user', 'JSON forbids date strings', 'The format is too long', 'It should be a number'], 0,
    'Missing offsets are one of the most common and most under-reported API contract defects.'],
  ['DP-M-004', 'unix',
    'You need the 20 most frequent error codes from a 4 GB log. The right approach is:',
    ['grep the pattern, cut the field, then sort | uniq -c | sort -rn | head -20', 'Open the file in an editor', 'Copy it to a spreadsheet', 'Read it with cat'], 0,
    'The standard pipeline streams the file instead of loading it, which is the difference between two seconds and a crashed editor.'],
  ['DP-M-005', 'virtualization',
    'Tests pass on the developer machine and fail in the container with date-format assertions. The first thing to check is:',
    ['The container TZ and locale environment variables', 'The container CPU limit', 'The Docker version', 'The network mode'], 0,
    'Containers commonly default to UTC and the C locale, which changes both formatting and parsing.'],
  ['DP-M-006', 'risks-in-testing',
    'Two days before release, a critical defect is found in an area that was descoped as low risk. The right response is:',
    ['Report it, reassess the risk model, and let the risk owner decide on the release', 'Fix it quietly', 'Delay the release unilaterally', 'Close it as out of scope'], 0,
    'A miss in the risk model is information about the model. Concealing it removes the decision from the person who owns it.'],
  ['DP-M-007', 'testing-metrics',
    'Automation coverage rose from 40% to 80%, but escaped defects did not fall. The most likely explanation is:',
    ['The new tests cover low-risk code or assert weakly', 'The metric is wrong', 'Escaped defects always lag by a year', 'Manual testing was reduced too little'], 0,
    'Coverage that does not target where defects escape adds runtime cost without adding detection.'],
  ['DP-M-008', 'api-theory',
    'A partner API begins returning an extra field in its response. Your suite fails on strict schema validation. The correct action is:',
    ['Confirm the contract, then relax the schema to allow additive changes if that is the agreement', 'Delete the schema validation', 'Ignore the failure', 'Block the release immediately'], 0,
    'Additive changes are usually allowed by contract. The schema should encode the actual contract, not the current response.'],
  ['DP-M-009', 'auto-web-ui',
    'A UI suite of 400 tests takes 90 minutes serially. The cheapest large win is usually:',
    ['Run in parallel across isolated workers with independent data', 'Rewrite the framework', 'Reduce the number of assertions', 'Increase the timeouts'], 0,
    'Parallelism converts wall-clock time into machine time. It only works if the data isolation is already there.'],
  ['DP-M-010', 'process-analysis',
    'Testers spend 40% of their time on environment problems. The most valuable improvement is:',
    ['Containerise the environment so it is reproducible and disposable', 'Hire more testers', 'Write more test cases', 'Reduce the scope of testing'], 0,
    'Forty percent of capacity is the largest single lever available, and it is a solved problem.'],
  ['DP-M-011', 'auto-db',
    'A nightly data migration test passes on a copy from January but fails on production data. The likely reason is:',
    ['The production data contains cases the snapshot does not, such as legacy nulls', 'The database is slower', 'The migration script is non-deterministic', 'The test framework is outdated'], 0,
    'Migrations fail on data variety, which is exactly what an old or trimmed snapshot lacks.'],
  ['DP-M-012', 'precise-estimation',
    'You estimated 5 days; after 3 days you are 30% done. The professional action is:',
    ['Report a revised forecast of about 10 days now, with the reason', 'Say nothing and try to catch up', 'Report on day 5', 'Reduce the scope silently'], 0,
    'At 30% in 60% of the budget, the trend is the forecast. Reporting it on day 3 leaves options; reporting it on day 5 leaves none.'],
  ['DP-M-013', 'testing-metrics',
    'Which pair of metrics together give the most honest picture of test effectiveness?',
    ['Defect detection percentage and escaped defect severity', 'Test cases executed and pass rate', 'Automation coverage and suite runtime', 'Hours logged and defects reported'], 0,
    'One measures how much was caught, the other measures how much what got through actually mattered.'],
  ['DP-M-014', 'auto-api',
    'You must test an endpoint that charges a card. In a shared test environment you should:',
    ['Use the provider sandbox with test cards and assert on the sandbox state', 'Use a real card with a small amount', 'Skip the test', 'Mock the whole endpoint and assert nothing'], 0,
    'Sandboxes exist precisely so that payment paths can be covered end to end without moving money.'],
  ['DP-M-015', 'unix',
    'A service is unresponsive. Which single command gives the most immediate diagnostic value?',
    ['journalctl -u <service> -n 200 --no-pager (or tail on its log)', 'ls -la /', 'df -h', 'whoami'], 0,
    'The recent log lines usually name the fault directly; disk and identity checks come after the log gives no answer.'],
]);
